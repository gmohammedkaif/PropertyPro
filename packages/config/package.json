{
  "name": "@propertypro/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared tooling presets (TypeScript base configs) for the PropertyPro monorepo.",
  "files": [
    "tsconfig.base.json"
  ]
}
